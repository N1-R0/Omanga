export const PAYMENTS_URL = "https://omanga.useinclude.com/";

/*
  `PLAN_CHECKOUT_URLS` moved to `config/site.ts` with the deletion of this
  group's `/plans` page. Its only consumer was that page; the redesigned
  `/plans` now reads the URLs from config, keyed by tier name rather than by
  lowercase slug.
*/
